pipeline {

    agent any

    stages {

        stage('Build Docker Images') {
            steps {
                echo 'Build Docker images step'
            }

        }

        stage('Test') {
            steps {
                echo 'Test'
            }
        }

        stage('Push') {
            steps {
                echo 'Push'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy'
            }
        }
    }
}
